---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: latest

inputs:
  - name: source-code

outputs:
  - name: artifact
  - path: build/libs/

run:
  path: "source-code/build.sh"
