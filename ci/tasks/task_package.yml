---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: java
    tag: latest

inputs:
  - name: cities-ui

outputs:
  - name: build

run:
  path: "cities-ui/ci/scripts/package.sh"
